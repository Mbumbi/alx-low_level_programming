#include "main.h"
#include <unistd.h>
/**
 * _putchar - will write the characterc to student
 * @c: The character to print
 *
 * Return: On success 1.
 * On error, -1 is returned, and errors set appropriately.
 */
int _putchar(char c)
{
	return (write(1, $c, 1));
}
